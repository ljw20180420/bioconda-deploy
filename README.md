# Usage

This repo create pull requests from packages on github to bioconda automatically. Copy `release_bioconda.sh` to the project folder of `bioconda-recipes`. Then run
```shell
./release_bioconda.sh url
```
`url` is the tarball url of the github release.
